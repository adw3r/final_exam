# adw3r.tech v1

Personal site built as a capstone project for the Beetroot Academy
frontend program (2025). Stack: React + Vite + TypeScript + SCSS,
with a Node.js API backend. Containerized via Docker Compose,
deployed behind nginx with TLS.

Superseded by v2 — see [link to current portfolio].
